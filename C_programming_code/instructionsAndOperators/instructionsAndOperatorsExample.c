#include<stdio.h>
#include<math.h>
float main(){
    // //type declaration and arithmetic instructions/statements
    // float a,b,c;
    // a=5;
    // b=5;
    // c=b;
    // float sum=a+b+c;
    // printf("sum =:%d\n",sum);


    // //multiple variables are assigned same value
    // float m,n,o,p;//Here we can't write float m=n=o=56 because 56 assigned to 'o' but at the same time compiler is unaware 
    // //of n
    // m=n=o=56;//but here we declared first and assigned later
    // printf("Vales are %d %d %d\n",m,n,o);





    // float x = 4;
    // float y = 5;
    // //float x+y=z;//Here z contains single value but x and y are two different value
    // //float z=xy;//It is allowed in mathematics but not in C program
    // float z=x+y;
    // float power=pow(y,x);
    // printf("\n%d\n%d\n",z,power);





    // //Modular operator
    // float number1,number2;
    // //number1=8.6;
    // //number2=4;
    // // printf("Enter both numbers\n");
    // // scanf("%d %d",&number1,&number2);
    // printf("Remainder of the numbers:%f", 1.6%1.2);



    

    //type conversion(intrinsic type conversion)
    // float num1=2;
    // float num2=3.2;
    //printf("%f",1.5*2.9);//It is wrong ,if we change the formate specifier to of float type then correct

    // float num= (float)1.9999999;
    // printf("The number is: %d",num);
    // return 0;
    
    //operator precedence

    // float a=10,b=11,c=12;
    // float d=a+b*c;
    // printf("The result is :\n%d",d);

    // //Operator of same precedence
    // float m=2,n=3,o=4,p=5;
    // float q=m*n/o*p;
    // printf("The result is :\n%d",q);

    

}